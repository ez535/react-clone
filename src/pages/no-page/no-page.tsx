import React, { FC } from 'react';
import { Typography } from 'antd';

const NoPage: FC = () => {
  return (
    <Typography.Title level={4}>Страница не найдена</Typography.Title>
  );
};

export default NoPage;